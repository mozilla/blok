{
  "name": "blok",
  "version": "1.0.0",
  "description": "Web Extension implementation of Firefox tracking protection",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "bundle": "browserify -d -t envify js/background.js > js/background.bundle.js && browserify -d -t envify -t [ babelify --presets [ react ] ] js/popup.js > js/popup.bundle.js",
    "build": "npm test && MODE=production npm run bundle && web-ext build",
    "build-dev": "npm test && MODE=dev npm run bundle && web-ext build",
    "coverage": "nyc npm test",
    "coveralls": "nyc npm test && nyc report --reporter=text-lcov | coveralls",
    "firefox": "npm run bundle && web-ext run",
    "lint": "standard && web-ext lint",
    "test": "tape tests/**/*.js | faucet",
    "watch": "npm-watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mozilla/blok.git"
  },
  "keywords": [
    "mozilla",
    "firefox",
    "tracking protection",
    "web extension"
  ],
  "author": "Luke Crouch",
  "license": "MPL-2.0",
  "bugs": {
    "url": "https://github.com/mozilla/blok/issues"
  },
  "homepage": "https://github.com/mozilla/blok#readme",
  "devDependencies": {
    "babel-preset-react": "6.11.1",
    "babelify": "7.3.0",
    "browserify": "13.0.1",
    "coveralls": "2.11.12",
    "envify": "3.4.1",
    "faucet": "0.0.1",
    "npm-watch": "0.1.5",
    "nyc": "7.0.0",
    "react": "15.3.1",
    "react-chrome-redux": "0.0.8",
    "react-dom": "15.3.1",
    "react-redux": "4.4.5",
    "redux": "3.5.2",
    "standard": "7.1.2",
    "tape": "4.6.0",
    "web-ext": "1.4.0"
  },
  "nyc": {
    "all": true,
    "exclude": [
      "**/*.bundle.js"
    ]
  },
  "standard": {
    "globals": [
      "BroadcastChannel",
      "browser",
      "fetch",
      "URL"
    ]
  },
  "watch": {
    "bundle": {
      "patterns": [
        "**/*.js"
      ],
      "ignore": "*bundle.js"
    }
  }
}
